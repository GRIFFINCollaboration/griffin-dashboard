include ../partials/head.jade

doctype html5
html
  head
    +head()
    script(type='text/javascript' src='static/xTags/clock/xClock.js')
    script(type='text/javascript' src='static/xTags/runControl/xStatus.js')
    link(rel="stylesheet" type="text/css" href='static/xTags/clock/xClock.css')
    link(rel="stylesheet" type="text/css" href='static/xTags/runControl/xStatus.css')


  body   
    div(id='wrapper')
      branding-head(experiment='GRIFFIN')

      div(id='mainContent')
        widget-nav(id='navBar' MIDAS= MIDAS)
        widget-status(id='statusBar', MIDAS= MIDAS)
        widget-clockControl(id='clockControl' MIDAS= MIDAS)
        widget-Clock(id='clockMonitor' MIDAS= MIDAS)

      div(id='push')

    branding-foot

    script.
      document.addEventListener('DOMComponentsLoaded', function(){
        //begin data fetch loop:
        rebootFetch();
      });